[project]
name = "embed-evals"
version = "0.1.0"
description = "Evaluation harness for comparing embedding models"
requires-python = ">=3.11"
dependencies = [
    "httpx",
    "pyyaml",
    "numpy",
    "scikit-learn",
    "rich",
    "typer",
]

[dependency-groups]
dev = ["ruff"]
